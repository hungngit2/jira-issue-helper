name: 'Jira Ticket Transition and Environment Info'
description: 'Fetches Jira ticket release information and updates the environment table in the ticket.'
inputs:
  JIRA_BASE_URL:
    description: 'Jira base URL, e.g. https://your-domain.atlassian.net'
    required: true
  JIRA_USER_EMAIL:
    description: 'Jira user email, e.g. yourname@your-domain'
    required: true
  JIRA_API_TOKEN:
    description: 'Jira API token'
    required: true
  ISSUE_KEY:
    description: 'Ticket key, e.g. ABC-1234. Works for both Jira and Linear - preferred over JIRA_ISSUE_KEY in new workflows.'
    required: false
  JIRA_ISSUE_KEY:
    description: 'Jira issue key, e.g. ABC-1234. Deprecated alias for ISSUE_KEY, kept for backward compatibility - use ISSUE_KEY instead in new workflows. Ignored when ISSUE_KEY is set.'
    required: false
  JIRA_ENV_COLUMNS:
    description: 'Comma separated columns for environment table, e.g. Environment,Branch,Path to Build,Path to Upsert'
    required: false
  JIRA_ISSUE_KEY_PATTERN:
    description: 'Default: ([A-Z0-9]+)[\\s-]?(\\d+)'
    required: false
  JIRA_ISSUE_TYPE_TRANSITION:
    description: 'Default: "Story:Code Review;Bug:Code Revieww"'
    required: false
  JIRA_COMMENT_BODY:
    description: 'Comment body to add to the Jira issue'
    required: false
  LINEAR_API_TOKEN:
    description: 'Linear API token. When set, Linear is tried first before falling back to Jira. If not set and ISSUE_TRACKER is explicitly "linear", JIRA_API_TOKEN is reused as the Linear token (for projects storing a real Linear token under that existing secret to avoid a workflow edit) - never as a silent default otherwise.'
    required: false
  LINEAR_ISSUE_KEY:
    description: 'Linear issue identifier, e.g. ENG-123. Defaults to ISSUE_KEY/JIRA_ISSUE_KEY if not set.'
    required: false
  LINEAR_COMMENT_BODY:
    description: 'Comment body to add to the Linear issue (falls back to JIRA_COMMENT_BODY if not set)'
    required: false
  ISSUE_TRACKER:
    description: 'Explicit tracker selection: "linear" or "jira". Omit to auto-detect (Linear if LINEAR_API_TOKEN is set, else Jira). When Linear is selected but the issue is not found, the action always falls back to Jira.'
    required: false
  ACTIONS_MODE:
    description: 'Mode of action to perform: "NewComment", "Transition", or "IssueInfo"'
    required: false
runs:
  using: node24
  main: 'dist/index.js'